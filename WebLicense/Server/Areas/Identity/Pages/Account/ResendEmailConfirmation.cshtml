@page
@model ResendEmailConfirmationModel
@using Microsoft.Extensions.Localization
@using WebLicense.Server
@inject IStringLocalizer<Global> ResG
@inject IStringLocalizer<ResendEmailConfirmationModel> ResL

@{
    ViewData["Title"] = ResL["Title"];
}

<div class="col-md-4">
    <h4>@ResL["Message.EnterEmail"]</h4>
    <hr />
    <form method="post">
        <div asp-validation-summary="ModelOnly"></div>
        <div class="form-floating mb-3">
            <input asp-for="Input.Email" class="form-control" placeholder="placeholder" />
            <label asp-for="Input.Email"></label>
            <span asp-validation-for="Input.Email"></span>
        </div>
        <button type="submit" class="btn btn-primary">@ResG["Text.Resend"]</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
